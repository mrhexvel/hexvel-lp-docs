---
title: Основы использования
description: Как начать работу с HEXVEL.
---

import { HelpCircle } from "lucide-react";
import { Callout } from "fumadocs-ui/components/callout";

# Основы использования

## Символика

- **±**: Команда работает с "+" (включить) или "−" (выключить).
- Пример: `±лайк` → `+лайк` (поставить лайк) или `-лайк` (убрать).

## Аргументы

- **()**: Обязательный параметр.
- **""**: Необязательный, с дефолтным значением.

## Переменные

| Переменная     | Описание                                                | Пример       |
| -------------- | ------------------------------------------------------- | ------------ |
| `ссылка`       | Линк или тег пользователя                               | `vk.com/id1` |
| `текст`        | Текст для команды                                       | `Привет`     |
| `кол-во`       | Число                                                   | `5`          |
| `фото`         | Прикреплённое изображение                               | -            |
| `город`        | указать город                                           | москва       |
| `интервал`     | указать время, после которого команда будет срабатывать | 5 мин        |
| `аудио `       | прикрепить аудиофайл                                    | -            |
| `префикс`      | указать префикс, который установлен в модуле            | мяу          |
| `тип префикса` | указать тип префикса для его изменения                  | команд       |
| `here/all`     | `here` - в одной беседе, `all` - во всех                | here         |
| `hex код`      | это обозначения цветов для виртуального интерфейса      | #FFFFFF      |

<Callout title="Подсказка" icon={<HelpCircle className="h-4 w-4 mt-[3px]" />}>
  Для подключения используйте [инструкцию](https://vk.cc/cJEaQV) или обратитесь
  к [поддержке](https://vk.cc/cJEcN8).
</Callout>
